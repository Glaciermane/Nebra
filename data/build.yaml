name: Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Build
        run: |
          echo "const password = '$SUPER_PASS';" > src/config.js
          #enojk

    env:
      SUPER_PASS: ${{ secrets.SUPER_PASS }}
